hl.config({
    input = {
        kb_layout = "gb";

        touchpad = {
            natural_scroll = true;
            scroll_factor = 0.5;
        };
    };
})

hl.gesture({ fingers = 3, direction = "horizontal", action = "workspace" })
